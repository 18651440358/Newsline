import '../../modal/user_modal.dart';

// 用户登录
class HandleUserLogin {
    final User user;
    HandleUserLogin(this.user);
}

// 用户登出
class HandleUserLogOut {
    final User user;
    HandleUserLogOut(this.user);
}